# 开发与验证

1. 从根目录执行与改动范围匹配的 Maven 构建和测试。
2. Starter 改动需验证自动配置是否按预期加载、是否可被关闭，以及缺少可选依赖时的行为。
3. 公共 API、配置项和默认行为变化需更新模块文档、示例和迁移说明。
4. 发布版本、合并发布分支和 Maven Central 发布均须按项目发布流程经开发者确认。

框架模块要避免隐式耦合应用服务；示例应位于 `examples` 仓库或相应测试中验证。
