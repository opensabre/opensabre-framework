spring:
  cloud:
    bus:
      trace:
        enabled: true
  sleuth:
    sampler:
      probability: 1.0
  mvc:
    throw-exception-if-no-handler-found: true
    format:
      date-time: yyyy-MM-dd HH:mm:ss
      date: yyyy-MM-dd
  servlet:
    multipart:
      max-request-size: "2MB"
      max-file-size: "2MB"
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss

opensabre:
  rest:
    result:
      framework:
        excludes: org.springdoc
      excludes:

logging:
  level:
    io.github.opensabre: info
  logback:
    rollingpolicy:
      max-file-size: 1GB
  file:
    path: logs/

