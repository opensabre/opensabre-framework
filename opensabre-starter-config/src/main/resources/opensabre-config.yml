spring:
  cloud:
    nacos:
      config:
        server-addr: ${REGISTER_HOST:localhost}:${REGISTER_PORT:8848}
        file-extension: yml
        # Spring Cloud Alibaba 2025.1 requires applications to import Nacos
        # configuration explicitly from application.yml via spring.config.import.
