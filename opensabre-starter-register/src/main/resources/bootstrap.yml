spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:localhost}:${REGISTER_PORT:8848}
        watch:
          enabled: true
        enabled: true
        cluster-name: ${CLOUD_AZ}  # 取环境变量AZ做为集群名